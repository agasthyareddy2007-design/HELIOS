import LandingPage from "@/components/landing/LandingPage";

/** Route: / — the HELIOS landing experience (Flow cinematic robot + story). */
export default function Page() {
  return <LandingPage />;
}
